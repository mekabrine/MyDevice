import UIKit
import AVFoundation

/// Minimal placeholder so the file exists and compiles.
/// This project’s UI uses PiPManager directly; you can expand this later if you want a custom overlay.
final class PiPOverlayViewController: UIViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
        view.backgroundColor = .black
    }
}